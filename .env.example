# このファイルを .env.local にコピーして値を埋めてください。
# .env.local はGitに含まれません(.gitignore済み)。

# 自分で作成したAWS IVSチャンネルの再生URL。
# AWSコンソール > Interactive Video Service > チャンネル で発行される
# 「Playback URL」をそのまま貼り付けます。
# 未設定の場合はAWS公式のデモ配信URLにフォールバックするため、
# 設定しなくてもアプリは動作します。
NEXT_PUBLIC_IVS_PLAYBACK_URL=
